export const disconnectFunction = () => {
    //Update the server when exiting the meeting
  };

  export const toggleMuteFunction = () => {
    //Update the server when one of the participants  clicks  the microphone
  };

  export const toggleCameraFunction = () => {
    //Update the server when one of the participants turned on the camera
  };

  export const SharingFunction = () => {
    //Update the server when one of the participants shared the screen
  };

